# sound-test — UART console audio test utility (RISC2 only, no LCD)
#
# Single C source compiled by the standard RISC2 build pipeline.

REPO_ROOT    = ../../
INCLUDE_DIRS = . $(ARCH_DIR)
VPATH_DIRS   = .
GAME_SRCS    = main.c

include $(REPO_ROOT)arch/risc2/build.mk
