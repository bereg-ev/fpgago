"""fpgago game library — local catalog, content classifier, source adapters.

Standalone (no PySide6 import) so the CLI runs headless; the desktop GUI
(D4 in ../PLAN.md) will drive the same Catalog + adapters.
"""
