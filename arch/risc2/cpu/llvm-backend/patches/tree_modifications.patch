diff --git a/clang/lib/Basic/CMakeLists.txt b/clang/lib/Basic/CMakeLists.txt
index adfc6ee32..ab3994f20 100644
--- a/clang/lib/Basic/CMakeLists.txt
+++ b/clang/lib/Basic/CMakeLists.txt
@@ -119,6 +119,7 @@ add_clang_library(clangBasic
   Targets/TCE.cpp
   Targets/VE.cpp
   Targets/WebAssembly.cpp
+  Targets/RISC2.cpp
   Targets/X86.cpp
   Targets/XCore.cpp
   Targets/Xtensa.cpp
diff --git a/clang/lib/Basic/Targets.cpp b/clang/lib/Basic/Targets.cpp
index 263253918..db7e313ec 100644
--- a/clang/lib/Basic/Targets.cpp
+++ b/clang/lib/Basic/Targets.cpp
@@ -37,6 +37,7 @@
 #include "Targets/TCE.h"
 #include "Targets/VE.h"
 #include "Targets/WebAssembly.h"
+#include "Targets/RISC2.h"
 #include "Targets/X86.h"
 #include "Targets/XCore.h"
 #include "Targets/Xtensa.h"
@@ -113,7 +114,9 @@ std::unique_ptr<TargetInfo> AllocateTarget(const llvm::Triple &Triple,
   llvm::Triple::OSType os = Triple.getOS();
 
   switch (Triple.getArch()) {
-  default:
+  case llvm::Triple::risc2:
+    return std::make_unique<targets::RISC2TargetInfo>(Triple, Opts);
+    default:
     return nullptr;
 
   case llvm::Triple::arc:
diff --git a/clang/lib/Basic/Targets/RISC2.cpp b/clang/lib/Basic/Targets/RISC2.cpp
new file mode 100644
index 000000000..65b35b361
--- /dev/null
+++ b/clang/lib/Basic/Targets/RISC2.cpp
@@ -0,0 +1,11 @@
+#include "RISC2.h"
+#include "clang/Basic/MacroBuilder.h"
+
+using namespace clang;
+using namespace clang::targets;
+
+void RISC2TargetInfo::getTargetDefines(const LangOptions &Opts,
+                                        MacroBuilder &Builder) const {
+  Builder.defineMacro("__risc2__");
+  Builder.defineMacro("__RISC2__");
+}
diff --git a/clang/lib/Basic/Targets/RISC2.h b/clang/lib/Basic/Targets/RISC2.h
new file mode 100644
index 000000000..4d0515d69
--- /dev/null
+++ b/clang/lib/Basic/Targets/RISC2.h
@@ -0,0 +1,52 @@
+//=== RISC2.h - Declare RISC2 target feature support -------*- C++ -*-===//
+#ifndef LLVM_CLANG_LIB_BASIC_TARGETS_RISC2_H
+#define LLVM_CLANG_LIB_BASIC_TARGETS_RISC2_H
+
+#include "clang/Basic/TargetInfo.h"
+#include "clang/Basic/TargetOptions.h"
+#include "llvm/ADT/StringRef.h"
+#include "llvm/TargetParser/Triple.h"
+
+namespace clang {
+namespace targets {
+
+class LLVM_LIBRARY_VISIBILITY RISC2TargetInfo : public TargetInfo {
+public:
+  RISC2TargetInfo(const llvm::Triple &Triple, const TargetOptions &Opts)
+      : TargetInfo(Triple) {
+    // 32-bit, big-endian instruction words, 24-bit address space
+    BigEndian = false;
+    TLSSupported = false;
+    PointerWidth = PointerAlign = 32;
+    IntWidth = IntAlign = 32;
+    LongWidth = LongAlign = 32;
+    LongLongWidth = 64;
+    LongLongAlign = 32;   // 32-bit CPU: 64-bit values are 4-byte aligned
+    resetDataLayout("e-p:32:32-i32:32-i64:32-n32-S32");
+  }
+
+  void getTargetDefines(const LangOptions &Opts,
+                         MacroBuilder &Builder) const override;
+
+  llvm::SmallVector<Builtin::InfosShard> getTargetBuiltins() const override {
+    return {};
+  }
+
+  ArrayRef<const char *> getGCCRegNames() const override { return {}; }
+  ArrayRef<TargetInfo::GCCRegAlias> getGCCRegAliases() const override {
+    return {};
+  }
+  bool validateAsmConstraint(const char *&Name,
+                              TargetInfo::ConstraintInfo &Info) const override {
+    return false;
+  }
+  std::string_view getClobbers() const override { return ""; }
+  BuiltinVaListKind getBuiltinVaListKind() const override {
+    return TargetInfo::VoidPtrBuiltinVaList;
+  }
+};
+
+} // namespace targets
+} // namespace clang
+
+#endif
diff --git a/llvm/CMakeLists.txt b/llvm/CMakeLists.txt
index fcbfed495..ad4d07ee0 100644
--- a/llvm/CMakeLists.txt
+++ b/llvm/CMakeLists.txt
@@ -536,6 +536,7 @@ set(LLVM_ALL_TARGETS
   WebAssembly
   X86
   XCore
+  RISC2
   )
 
 set(LLVM_ALL_EXPERIMENTAL_TARGETS
diff --git a/llvm/include/llvm/TargetParser/Triple.h b/llvm/include/llvm/TargetParser/Triple.h
index 9c83abeeb..042e8f7cc 100644
--- a/llvm/include/llvm/TargetParser/Triple.h
+++ b/llvm/include/llvm/TargetParser/Triple.h
@@ -110,7 +110,9 @@ public:
     renderscript32, // 32-bit RenderScript
     renderscript64, // 64-bit RenderScript
     ve,             // NEC SX-Aurora Vector Engine
-    LastArchType = ve
+    risc2,          // RISC2 custom FPGA CPU
+
+    LastArchType = risc2
   };
   enum SubArchType {
     NoSubArch,
diff --git a/llvm/lib/TargetParser/Triple.cpp b/llvm/lib/TargetParser/Triple.cpp
index a4f9dd42c..fda4fc19d 100644
--- a/llvm/lib/TargetParser/Triple.cpp
+++ b/llvm/lib/TargetParser/Triple.cpp
@@ -23,6 +23,7 @@ using namespace llvm;
 
 StringRef Triple::getArchTypeName(ArchType Kind) {
   switch (Kind) {
+    case risc2:           return "risc2";
   case UnknownArch:    return "unknown";
 
   case aarch64:        return "aarch64";
@@ -670,7 +671,8 @@ static Triple::ArchType parseArch(StringRef ArchName) {
                   "dxilv1.9"},
                  Triple::dxil)
           .Case("xtensa", Triple::xtensa)
-          .Default(Triple::UnknownArch);
+          .Case("risc2", Triple::risc2)
+    .Default(Triple::UnknownArch);
 
   // Some architectures require special parsing logic just to compute the
   // ArchType result.
@@ -1059,6 +1061,9 @@ static Triple::ObjectFormatType getDefaultFormat(const Triple &T) {
 
   case Triple::dxil:
     return Triple::DXContainer;
+
+  case Triple::risc2:
+    return Triple::ELF;
   }
   llvm_unreachable("unknown architecture");
 }
@@ -1754,6 +1759,7 @@ unsigned Triple::getArchPointerBitWidth(llvm::Triple::ArchType Arch) {
   case llvm::Triple::x86:
   case llvm::Triple::xcore:
   case llvm::Triple::xtensa:
+  case llvm::Triple::risc2:
     return 32;
 
   case llvm::Triple::aarch64:
@@ -2019,6 +2025,7 @@ Triple Triple::getBigEndianArchVariant() const {
   case Triple::ve:
   case Triple::csky:
   case Triple::xtensa:
+  case Triple::risc2:   // risc2 has no big-endian variant
 
   // ARM is intentionally unsupported here, changing the architecture would
   // drop any arch suffixes.
@@ -2088,6 +2095,7 @@ Triple Triple::getLittleEndianArchVariant() const {
     break;
   case Triple::sparc:      T.setArch(Triple::sparcel);  break;
   case Triple::tce:        T.setArch(Triple::tcele);    break;
+  case Triple::risc2:      break; // already little-endian
   default:
     llvm_unreachable("getLittleEndianArchVariant: unknown triple.");
   }
@@ -2140,6 +2148,7 @@ bool Triple::isLittleEndian() const {
   case Triple::x86_64:
   case Triple::xcore:
   case Triple::xtensa:
+  case Triple::risc2:
     return true;
   default:
     return false;
